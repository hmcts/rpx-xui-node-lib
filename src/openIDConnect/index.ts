// import {Client, ClientMetadata, Issuer, Strategy, TokenSet, UserinfoResponse} from 'openid-client';
import { IAuthOptions } from '../auth/interfaces';

export function handleOpenIdConnect(options: IAuthOptions) {
    // code for the OpenId connect
}
